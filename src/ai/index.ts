// Local barrel for the AI authoring path (TASK-004).
// Re-exported from the package entry (src/index.ts) by the orchestrator.
export {
  generatePostBody,
  DEFAULT_AI_MODEL,
  type GeneratePostOptions,
  type GeneratedPost,
} from './generatePostBody'
